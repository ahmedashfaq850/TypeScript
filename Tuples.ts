// first in tuples order is matter if we specifiy type in square bracket
// fixed length
let users: [string, number, string]

users = ['ahmed',23,'ahmed']

// but the twist is you can add elemeny by arr functions like push, unshift, 


export {}